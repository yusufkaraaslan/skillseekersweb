---
import Header from '../components/astro/layout/Header.astro';
import Footer from '../components/astro/layout/Footer.astro';
import JsonLd from '../components/seo/JsonLd.astro';
import Analytics from '@vercel/analytics/astro';
import '../styles/global.css';
import { getLangFromUrl } from '../i18n/utils';

interface Props {
  title?: string;
  description?: string;
  ogImage?: string;
  hasDocsSidebar?: boolean;
}

const {
  title = 'Skill Seekers - Transform Docs into Claude AI Skills',
  description = 'Automatically convert documentation websites, GitHub repositories, and PDFs into production-ready Claude AI skills in minutes. Supports Claude, Gemini, OpenAI, and more.',
  ogImage = '/og-image.png',
  hasDocsSidebar = false,
} = Astro.props;

const lang = getLangFromUrl(Astro.url);
const canonicalURL = new URL(Astro.url.pathname, Astro.site);
const fullOgImage = new URL(ogImage, Astro.site);

// Generate alternate URLs for hreflang
function getAlternateUrl(targetLang: string, currentPath: string): URL {
  if (targetLang === 'en') {
    // Remove /zh/ prefix if present
    const cleanPath = currentPath.replace(/^\/zh\//, '/');
    return new URL(cleanPath === '' ? '/' : cleanPath, Astro.site);
  } else {
    // Add /zh/ prefix if not present
    if (currentPath.startsWith('/zh/')) {
      return new URL(currentPath, Astro.site);
    }
    const newPath = currentPath === '/' ? '/zh/' : `/zh${currentPath}`;
    return new URL(newPath, Astro.site);
  }
}

const currentPath = Astro.url.pathname;
const enUrl = getAlternateUrl('en', currentPath);
const zhUrl = getAlternateUrl('zh', currentPath);
---

<!doctype html>
<html lang={lang} class="dark">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <link rel="canonical" href={canonicalURL} />

    <!-- hreflang tags for multi-language SEO -->
    <link rel="alternate" hreflang="en" href={enUrl} />
    <link rel="alternate" hreflang="zh" href={zhUrl} />
    <link rel="alternate" hreflang="x-default" href={enUrl} />

    <meta name="generator" content={Astro.generator} />

    <!-- Primary Meta Tags -->
    <title>{title}</title>
    <meta name="title" content={title} />
    <meta name="description" content={description} />

    <!-- Open Graph / Facebook -->
    <meta property="og:type" content="website" />
    <meta property="og:url" content={canonicalURL} />
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    <meta property="og:image" content={fullOgImage} />
    <meta property="og:locale" content={lang === 'zh' ? 'zh_CN' : 'en_US'} />
    <meta property="og:locale:alternate" content={lang === 'zh' ? 'en_US' : 'zh_CN'} />

    <!-- Twitter -->
    <meta property="twitter:card" content="summary_large_image" />
    <meta property="twitter:url" content={canonicalURL} />
    <meta property="twitter:title" content={title} />
    <meta property="twitter:description" content={description} />
    <meta property="twitter:image" content={fullOgImage} />

    <!-- Theme Color -->
    <meta name="theme-color" content="#0a0a0f" />

    <!-- Preconnect to Google Fonts -->
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&family=JetBrains+Mono:wght@400;500&family=Space+Grotesk:wght@500;600;700&display=swap" rel="stylesheet" />

    <!-- JSON-LD Structured Data -->
    <JsonLd type="Organization" data={{ description }} />
    <JsonLd type="WebSite" />
  </head>
  <body class="min-h-screen flex flex-col">
    <Header />
    <main class="flex-grow">
      <slot />
    </main>
    <Footer hasDocsSidebar={hasDocsSidebar} />
    <Analytics />
  </body>
</html>
